version: 0.1             
component: build
timeoutInSeconds: 1000
shell: bash           

steps:
  - type: Command
    name: "Maven install"
    command: |
      mvn package spring-boot:repackage

outputArtifacts:                  
  - name: output1           
    type: BINARY       
    location: /workspace/my_source_git01/pom.xml